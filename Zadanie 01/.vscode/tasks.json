{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Decoder.class",
			"type": "shell",
			"options": {
				"cwd": "${workspaceFolder}\\bin"
			},
			"command": "javac ../src/*.java; mv -Force ../src/*.class ./; java Tester",
			"group": {
		  		"kind": "build",
		  		"isDefault": true
			}
	  	}
	]
}